# Django and core dependencies
Django>=5.0
python-decouple
python-dotenv
psycopg2-binary

# Django REST Framework and API
djangorestframework
djangorestframework-simplejwt
django-cors-headers
drf-spectacular  # API documentation (OpenAPI/Swagger)

# Celery and task queue
celery
redis

# File conversion libraries
pdf2docx
docx2pdf
PyPDF2  # PDF validation and inspection
Pillow
moviepy
python-pptx
openpyxl
pdfplumber
reportlab

# Windows-only dependencies (for PowerPoint COM automation)
# Install on Windows only: pip install pywin32
# pywin32  # Uncomment on Windows for PowerPoint COM automation

# HTTP and webhook utilities
requests

# Additional utilities
#python-magic-bin  # For Windows MIME type detection
python-magic
django-redis

#sudo apt-get install libreoffice
unoconv

# Testing libraries
hypothesis  # Property-based testing

# ==============================================================================
# EXTERNAL SYSTEM REQUIREMENTS (Not Python packages)
# ==============================================================================
#
# LibreOffice (Required for DOCX/PPTX to PDF conversion on Linux/Mac)
# ------------------------------------------------------------------------------
# Ubuntu/Debian:
#   sudo apt update
#   sudo apt install -y libreoffice libreoffice-writer libreoffice-calc libreoffice-impress
#   sudo apt install -y libreoffice-core libreoffice-common
#
# RHEL/CentOS:
#   sudo yum install -y libreoffice
#
# Mac:
#   brew install libreoffice
#   # Or download from: https://www.libreoffice.org/download/
#
# Windows:
#   Download and install from: https://www.libreoffice.org/download/
#   # Or use Microsoft PowerPoint (preferred on Windows)
#
# Verify installation:
#   libreoffice --version
#   libreoffice --headless --convert-to pdf test.docx
#
# ------------------------------------------------------------------------------
# System Utilities (Required for LibreOffice on minimal Linux installations)
# ------------------------------------------------------------------------------
# Ubuntu/Debian:
#   sudo apt install -y coreutils grep sed findutils
#
# ------------------------------------------------------------------------------
# MIME Type Detection (Required for file validation)
# ------------------------------------------------------------------------------
# Ubuntu/Debian:
#   sudo apt install -y libmagic1
#
# RHEL/CentOS:
#   sudo yum install -y file-libs
#
# ------------------------------------------------------------------------------
# Production Server (Gunicorn)
# ------------------------------------------------------------------------------
# Install separately for production:
#   pip install gunicorn
#
# ==============================================================================

#sudo apt install -y libmagic1
#pip install -r requirements.txt
#sudo apt install -y libreoffice
#sudo apt install -y libreoffice libmagic1 unoconv 



